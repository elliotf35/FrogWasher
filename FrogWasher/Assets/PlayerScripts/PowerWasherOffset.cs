using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerWasherOffset : MonoBehaviour
{
    public float xOffset = 0.21f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // xOffset = 0.21f;
        // if (transform.parent.rotation.y == 1) {
        //     xOffset = -0.21f;
        // }
        // transform.position = new Vector2(transform.parent.position.x + xOffset, transform.parent.position.y);
    }
}
